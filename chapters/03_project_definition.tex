% ============================================================
% 3. PROJECT DEFINITION
% ============================================================

\begin{sectionintro}{3}{Project Definition}{
  \begin{itemize}[leftmargin=1.5em]
    \item Gateway server architecture and components
    \item Command-line interface for database access
    \item Web administration dashboard
    \item Project scope and deliverables
  \end{itemize}
}
zGate is a Zero Trust database access gateway platform eliminating credential exposure through identity-based access control. This chapter defines the six-component architecture comprising the gateway server for protocol-aware proxying, the CLI for secure OIDC-authenticated developer access, the Next.js web dashboard for centralized administration, the AI masking engine, the T-SQL parser sidecar, and the Vault-first deployment stack. We detail each component's capabilities and establish clear project scope boundaries.
\end{sectionintro}

\section{Project Definition and Scope}
zGate is a comprehensive Zero Trust database access gateway platform designed to eliminate credential exposure, enforce identity-based access control, and provide complete auditability for database operations in enterprise environments. The project encompasses the design, implementation, and deployment of a six-component architecture that intercepts, authenticates, authorizes, and audits all database access in real-time.

\subsection{System Components}
The platform consists of six integrated components working in concert:

\subsubsection{Gateway Server (zGate Core)}
\begin{itemize}
    \item \textbf{Protocol Handlers:} Native implementation of MySQL, PostgreSQL, and MSSQL wire protocols, enabling transparent protocol-level interception and inspection of database traffic
    \item \textbf{Authentication System:} OIDC Device Flow (CLI) and PKCE browser flow (WebUI) via Keycloak; JWT-based internal session tokens (HS256, 5-minute access / 1-hour refresh), eliminating the need for users to possess database credentials
    \item \textbf{Interceptor Pipeline:} Eight-stage middleware pipeline per query: RoutingBoundary \textrightarrow{} RateLimit \textrightarrow{} Safety (T-SQL AST) \textrightarrow{} DynamicMasking \textrightarrow{} AIMasking \textrightarrow{} RowMasking \textrightarrow{} Anomaly (ML Ensemble) \textrightarrow{} Logging
    \item \textbf{Policy Engine:} Real-time policy evaluation engine performing fresh permission lookups for every database operation, supporting role-based and custom permission models; six compliance frameworks pre-seeded (GDPR, HIPAA, PCI-DSS, SOX, ISO~27001, NIST)
    \item \textbf{Built-in PKI:} Internal CA auto-initialized at startup; CSR-based mTLS certificate issuance; revocation cache for instant session kill
    \item \textbf{Session Management:} Per-user, per-database session tracking with context preservation and graceful cleanup mechanisms
\end{itemize}

\subsubsection{Command-Line Interface (zGate-CLI)}
\begin{itemize}
    \item \textbf{OIDC Device Authorization Grant:} Browser-based authentication without CLI password exposure; Keycloak polls and returns OIDC tokens exchanged for internal zGate session tokens
    \item \textbf{Bubble Tea TUI:} Stateful interactive terminal dashboard with Model/Update/View loop; headless subcommands for CI/CD pipelines
    \item \textbf{Token Storage \& Management:} Cross-platform secure token storage using OS keyring (Windows Credential Manager, macOS Keychain, Linux Secret Service) with encrypted file fallback
    \item \textbf{mTLS Certificate Lifecycle:} CSR-first issuance; private key never leaves client machine; auto-renewal at 25\% lifetime threshold or CA rotation
    \item \textbf{Local Tunnel (Stunnel Pattern):} Loopback listener (Unix socket on Linux/macOS, TCP on Windows) bridges plaintext database client to TLS~1.3 mTLS proxy connection
\end{itemize}

\subsubsection{Web Administration Dashboard (zGate-WebUI)}
Built with Next.js~15, React~19, Tailwind CSS~4, and Radix UI, the dashboard provides comprehensive management capabilities via AES-256 iron-session and Vault AppRole secrets:
\begin{itemize}
    \item \textbf{Overview Dashboard:} Real-time system metrics (6 endpoints via \texttt{Promise.allSettled()}), active sessions, and ML anomaly alert counts
    \item \textbf{Database Management:} Configure database connections with five TLS modes; live connectivity testing
    \item \textbf{User Management:} Create, modify, and deactivate user accounts with role assignments; pending-sync state for offline targets
    \item \textbf{Administrator Management:} Four-tier RBAC (super / db\_admin / policy\_admin / auditor) with immutable usernames and two-phase privilege escalation
    \item \textbf{Access Control:} Roles referencing safety templates, masking templates, AI masking templates, and row-filter rules by Template ID
    \item \textbf{Active Sessions Monitoring:} Live list of proxied connections; instant one-click TCP-level session revocation
    \item \textbf{Query Policies:} Visual DSL editor for safety rules (allow/deny/warn/mask), rate limits, column masking, and row masking
    \item \textbf{Compliance Templates:} One-framework-per-database constraint; six pre-seeded regulatory bundles (GDPR, HIPAA, PCI-DSS, SOX, ISO~27001, NIST)
    \item \textbf{Pending Reviews:} Warn-level query approval queue with 3-second polling and 5-minute hard timeout
    \item \textbf{AI/LLM Settings:} Hot-swappable LLM providers; write-only API key; test-before-save verification
    \item \textbf{Activity Audit:} Two-tier NDJSON forensic drill-down; ML risk\_score red-badge highlighting
    \item \textbf{Service Accounts:} Dual authenticators (Bearer + mTLS); zero-knowledge secret reveal; expiry proactive warnings
    \item \textbf{Shared Account Pools:} Role-bound pooled credentials; real-time concurrency utilisation bar
\end{itemize}

\subsubsection{AI Engine (zGate-AI)}
\begin{itemize}
    \item \textbf{Framework:} Python 3.12 FastAPI service exposing two primary capabilities
    \item \textbf{Column Classification:} \texttt{/classify-columns} endpoint for LLM-based column sensitivity mapping and masking strategy selection
    \item \textbf{Anomaly Detection:} \texttt{/anomaly/detect} endpoint powered by an ML ensemble behavioral scoring system
    \item \textbf{Pluggable LLMs:} Support for Anthropic, Gemini, OpenAI-compatible, and Ollama providers
    \item \textbf{Fail-safes:} Fail-open for anomaly detection (scoring error never blocks traffic) vs fail-closed for masking (classification timeout withholds data)
    \item \textbf{Security \& Retraining:} Vault AppRole authentication and online retraining via admin feedback labels
\end{itemize}

\subsubsection{T-SQL Parser Sidecar (zGate-TSQL-Parser)}
\begin{itemize}
    \item \textbf{Framework:} C\# .NET 8 gRPC sidecar using Microsoft ScriptDOM for AST analysis
    \item \textbf{Structural Analysis:} Extraction and analysis of 40+ T-SQL structural features
    \item \textbf{Threat Detection:} Identification of SQL injection bypass patterns and data exfiltration vectors
    \item \textbf{Query Fingerprinting:} Normalization and SHA-256 fingerprint generation for queries
    \item \textbf{Masking Resolution:} Output column tracing for SELECT statements to apply masking rules
    \item \textbf{Architecture:} Exposed via gRPC to the Server, running as a separate container alongside the proxy
\end{itemize}

\subsubsection{Deployment Orchestrator (zGate-Deploy)}
\begin{itemize}
    \item \textbf{Orchestration Layer:} Docker Compose orchestration with two branches (dev posture vs Azure production)
    \item \textbf{Provisioned Services:} Keycloak for OIDC, Vault with auto-unseal sidecar, Caddy for TLS termination, and OTel stack (Collector, Prometheus, Grafana, Loki, Tempo, Alertmanager)
    \item \textbf{Bootstrapping:} Keycloak realm automatically hydrated from JSON at first boot
    \item \textbf{Installation:} Self-hosted installer optimized for offline deployments
\end{itemize}

\subsection{Scope Boundaries}

\subsubsection{In Scope:}
\begin{itemize}
    \item MySQL, Postgres and MSSQL protocol support with complete authentication and query interception
    \item Role-based access control with hierarchical permission models
    \item Temporary database user lifecycle management
    \item JWT-based authentication with automatic token refresh
    \item Dynamic proxy allocation and connection pooling
    \item Comprehensive audit logging of all database operations
    \item RESTful API for programmatic access
    \item Cross-platform CLI with secure credential storage
    \item Web-based administration interface
    \item Configuration-driven deployment (YAML-based)
    \item Session-level access control and monitoring
\end{itemize}

\subsubsection{Out of Scope (Current Phase):}
\begin{itemize}
    \item Oracle and other non-SQL-wire database protocol support (planned for future phases)
    \item Integration with SAML or LDAP identity providers (Keycloak OIDC is the current IDP)
    \item Database activity replay and forensic analysis tools
    \item High-availability and clustering capabilities for the gateway server
    \item Advanced query rewriting and optimization
    \item Automated compliance report generation and export
\end{itemize}

\section{Objectives}
The zGate project aims to achieve the following measurable technical and security objectives:

\subsection{Core Security Objectives}

\subsubsection{Eliminate Direct Credential Exposure}
\begin{itemize}
    \item Implement JWT-based authentication system where users never handle production database passwords
    \item Develop automatic temporary database user creation with session-specific, cryptographically secure credentials
    \item Ensure temporary credentials are purged immediately upon session termination, preventing credential accumulation
    \item Achieve zero instances of production credentials appearing in user configuration files, environment variables, or CLI history
\end{itemize}

\subsubsection{Enforce Identity-Based Access Control}
\begin{itemize}
    \item Build a role-based access control (RBAC) engine supporting hierarchical roles and custom permissions
    \item Implement real-time permission evaluation that reflects role changes immediately without requiring user re-authentication
    \item Support per-database, per-user authorization with multiple privilege levels (read-only, read-write, admin)
    \item Enable policy decisions based on fresh configuration lookups rather than stale token claims
\end{itemize}

\subsubsection{Implement Protocol-Level Interception}
\begin{itemize}
    \item Develop native protocol handlers for PostgreSQL, MySQL and MSSQL wire protocols
    \item Enable transparent database proxy functionality allowing standard client tools (MySQL Workbench, SSMS) to function without modification
    \item Intercept and route all database traffic through the gateway with sub-100ms latency overhead for typical operations
    \item Maintain protocol compatibility ensuring 100\% of standard database operations work through the proxy
\end{itemize}

\subsubsection{Provide Comprehensive Audit Logging}
\begin{itemize}
    \item Log all authentication attempts with user identity, timestamp, success/failure status, and failure reasons
    \item Record complete session lifecycle: establishment, duration, database target, and termination circumstances
    \item Capture connection metadata sufficient for forensic investigation and compliance verification
    \item Generate structured logs compatible with standard log aggregation and SIEM tools
\end{itemize}

\subsection{Operational Objectives}

\subsubsection{Deliver Multi-Component Integration}
\begin{itemize}
    \item Build RESTful API server exposing authentication, database listing, connection management, and session control
    \item Develop cross-platform CLI with secure token storage using OS keyring and automatic token refresh
    \item Create responsive web administration dashboard supporting all management functions
    \item Ensure seamless data flow between all three components (Gateway, CLI, WebUI)
\end{itemize}

\subsubsection{Enable Centralized Administration}
\begin{itemize}
    \item Provide web-based interface for managing users, roles, database connections, and access policies
    \item Support real-time monitoring of active sessions with ability to view connection details and terminate sessions
    \item Allow administrators to modify permissions and configurations without system restarts
    \item Implement user and admin account separation with appropriate privilege boundaries
\end{itemize}

\subsubsection{Support Multi-Database Environments}
\begin{itemize}
    \item Design modular architecture allowing support for MySQL, PostgreSQL, and MSSQL through unified interfaces
    \item Enable addition of new database protocols through implementation of standard handler and manager interfaces
    \item Maintain per-database configuration for credentials, connection parameters, and access policies
    \item Support simultaneous connections to multiple database instances of different types
\end{itemize}

\subsubsection{Ensure Production-Ready Stability}
\begin{itemize}
    \item Implement graceful error handling, connection cleanup, and resource management
    \item Support concurrent sessions from multiple users without resource conflicts
    \item Provide configuration validation and startup checks to prevent misconfiguration
    \item Enable zero-downtime configuration reloads for non-breaking changes
\end{itemize}

\subsection{Academic and Technical Learning Objectives}

\subsubsection{Demonstrate Applied Security Architecture}
\begin{itemize}
    \item Apply Zero Trust principles specifically to database access management
    \item Implement secure authentication flows with proper token lifecycle management
    \item Design authorization systems with clear separation between authentication, policy evaluation, and enforcement
    \item Practice secure coding including input validation, SQL injection prevention, and secure credential generation
\end{itemize}

\subsubsection{Develop Full-Stack System Integration Skills}
\begin{itemize}
    \item Build high-performance network services in Go with concurrent connection handling
    \item Implement database wire protocol parsers and connection proxies
    \item Create modern React-based web interfaces with real-time data updates
    \item Develop CLI applications with cross-platform compatibility and user experience focus
    \item Design and document RESTful APIs for inter-component communication
\end{itemize}


